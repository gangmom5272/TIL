## Database / MySQL

### 오늘 한 공부

- 생활코딩 DATABASE2 - MySQL 학습
- MySQL 서버 접속
- 데이터베이스 생성 / 삭제 / 조회 / 선택 실습

### 스키마와 데이터베이스

스키마는 서로 연관된 데이터들을 그룹핑하는 단위이다.

MySQL에서는 `CREATE DATABASE`로 스키마를 생성할 수 있다.  
여러 스키마를 저장하고 관리하는 시스템이 데이터베이스 서버이다.

### 데이터베이스 서버의 장점

데이터베이스 서버는 보안 체계를 제공한다.  
사용자별로 권한을 부여할 수 있어서 데이터를 안전하게 관리할 수 있다.

### MySQL 접속

```bash
cd /usr/local/mysql/bin
./mysql -uroot -p